eureka:
  client:
    service-url:
     defaultZone: http://localhost:8671/eureka
spring:
  application:
    name: api-gateway
  cloud:
    config:
      discovery:
        enabled: true
        server-id: CONFIG
      profile: dev